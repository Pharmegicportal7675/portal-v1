# Hostinger MySQL (primary database)
DATABASE_URL=mysql://USER:PASSWORD@HOST:3306/DATABASE

# Source Supabase PostgreSQL (for one-time data migration only)
SUPABASE_DATABASE_URL=postgresql://postgres:PASSWORD@db.PROJECT.supabase.co:5432/postgres

# SMTP2GO — do not change
SMTP_HOST=mail.smtp2go.com
SMTP_PORT=587
SMTP_USER=your-smtp2go-user
SMTP_PASS=your-smtp2go-password
SMTP_FROM=Pharmegic Healthcare <noreply@pharmegichealthcare.com>

# App
NEXT_PUBLIC_APP_URL=https://portal.pharmegichealthcare.com

# Legacy (no longer required for DB — kept for reference during migration)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE=
